import { RoutePlaceholder } from '@/shared/ui/RoutePlaceholder'

export function RosterSelectionPage() {
  return (
    <RoutePlaceholder
      title="Selección de Roster"
      description="/roster/:gameId — Fase 2: lineup y deck táctico pre-partida"
    />
  )
}
