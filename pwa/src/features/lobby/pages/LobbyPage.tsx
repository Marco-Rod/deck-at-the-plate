import { RoutePlaceholder } from '@/shared/ui/RoutePlaceholder'

export function LobbyPage() {
  return (
    <RoutePlaceholder title="Lobby" description="/lobby — Fase 2: modos de juego y rivales CPU" />
  )
}
